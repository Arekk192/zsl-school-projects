// import { Animal, animalsArray, addAnimal } from "./model";

const controller = {
  //   add: (animal) => {
  //     addAnimal(new Animal(animal.name, animal.color));
  //   },
  delete: (id) => {
    // usuwanie po id z animalsArray
  },
  update: (id) => {
    // update po id elementu animalsArray
  },
  getall: () => {
    return animalsArray;
  },
};
export default controller;
